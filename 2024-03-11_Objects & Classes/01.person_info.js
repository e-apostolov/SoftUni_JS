function solve(firstNme, lastName, age) {
    const personInfo = {
        firstName: firstNme,
        lastName: lastName,
        age: age,
    }
    return personInfo;
};

solve('Peter', 'Pan', '20')